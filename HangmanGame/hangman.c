#include <stdio.h>

int main()
{
    char secretWord[20];
    sprintf(secretWord, "WATERMELON");

    // secretWord[10] = '\0';
    // printf("%s\n", secretWord);

    int guessed = 0;
    int hanged = 0;

    do
    {

    } while (!guessed && !hanged);
}